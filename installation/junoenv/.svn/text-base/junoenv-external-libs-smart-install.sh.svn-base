#!/bin/bash

function juno-ext-libs-smart-install() {
    local msg="==== $FUNCNAME: "
    local pkg=$1; shift
    local ver=$1; shift

    echo $msg smart install $pkg $ver
    juno-ext-libs-all $pkg
}
