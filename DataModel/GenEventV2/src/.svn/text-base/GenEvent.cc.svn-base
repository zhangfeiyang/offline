#include "Event/GenEvent.h"

ClassImp(JM::GenEvent);

namespace JM
{
    GenEvent::GenEvent() {
        evt = 0;
    }

    GenEvent::~GenEvent() {
        if (evt) {
            delete evt;
        }
    }
}
